@model Lang_Blog_Podcast_Website_ASPdotNET.Models.Story

@{
    ViewData["Title"] = Model.Title;
}

<link rel="stylesheet" href="~/css/Story/detail.css" />

<div class="container article-container">
    <div class="row custom-editorial-layout">

        <!-- CỘT TRÁI: SIDEBAR THÔNG TIN -->
        <div class="col-md-3 sidebar-column">
            <div class="sidebar-meta">
                <a href="@Url.Action("Index", "Story")" class="back-link">
                    &mdash; QUAY LẠI CÂU CHUYỆN
                </a>

                <div class="meta-group">
                    <span class="meta-label">DANH MỤC</span>
                    <span class="meta-value font-italic">@(Model.Category?.Name ?? "Tâm sự")</span>
                </div>

                <div class="meta-group">
                    <span class="meta-label">NGÀY XUẤT BẢN</span>
                    <span class="meta-value">@Model.CreatedAt.ToString("dd.MM.yyyy")</span>
                </div>

                <div class="meta-group">
                    <span class="meta-label">TÁC GIẢ</span>
                    <span class="meta-value font-italic">@(string.IsNullOrEmpty(Model.Author) ? "Ẩn danh" : Model.Author)</span>
                </div>

                <!-- 💡 ĐÃ XÓA KHỐI ARTICLE-ACTIONS (LƯU TRANG & CHIA SẺ) TẠI ĐÂY -->
            </div>
        </div>

        <!-- CỘT PHẢI: NỘI DUNG CHÍNH -->
        <div class="col-md-9 content-column">

            <div class="article-header">
                <span class="category-badge">@(Model.Category?.Name?.ToUpper() ?? "TÂM SỰ")</span>
                <h1 class="article-title">@Model.Title</h1>
                <!-- 💡 ĐÃ XÓA KHỐI ARTICLE-STATS (LƯỢT XEM & BÌNH LUẬN) TẠI ĐÂY -->
            </div>

            <!-- NỘI DUNG CHỮ CHẠY LIỀN MẠCH NGAY DƯỚI TIÊU ĐỀ -->
            <div class="article-body">
                @Html.Raw(Model.Content)
            </div>

            <hr class="article-divider" />

            <!-- FOOTER BÀI VIẾT -->
            <div class="article-footer">
                <div class="author-note">
                    <span class="note-label">LỜI NGỎ</span>
                    <p>Cảm ơn bạn đã dành thời gian lắng nghe tâm hồn của mình.<br>Hy vọng bài viết này mang lại cho bạn chút ánh sáng nhỏ bé<br>trong thế giới đầy hối hả này.</p>
                </div>
                <div class="magazine-signature">
                    <span class="note-label">THỰC HIỆN BỞI</span>
                    <h4>Lặng. Magazine</h4>
                </div>
            </div>

        </div>
    </div>
</div>